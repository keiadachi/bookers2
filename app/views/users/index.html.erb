<main>
<p id="notice"></p>

<div class="container px-5 px-sm-0">
  <div class="row">

  <div class="col-md-3">
    <h3>user info</h3>
    <%= attachment_image_tag @user, :profile_image, format: 'jpeg', fallback: 'no_image.jpg', size:'100x100', class: "attachment user profile_image fallback"  %>
      <table class="table">
      <tbody>
        <tr><th>name</th><th><p><%= @user.name %></p></th></tr>
        <tr><th>introduction</th><th><p><%= @user.introduction %></p></th></tr>
      </tbody>
      </table>
  <div class="row">
    <% if @user.id == current_user.id %>
    <p><%= link_to "プロフィール編集", edit_user_path(@user), class:"btn btn-outline-secondary btn-block fas fa-user-cog" %></p>
    <% end %>
  </div>


<h2 class="mt-3">New book</h2>
  <%#<%= form_with model:@book, local: true do |f| %>
 <form action="/books" accept-charset="UTF-8" method="post">
   <input name="utf8" type="hidden" value="">
   <input type="hidden" name="authenticity_token" value="5DVd2mJB42s+jbfIHwNLXuPHujifM9DA335BBZUcVvx6JXt9pBdY/MjSq+hO3T80Sq4sk5yWTonR3gvUdL0S3g==">

   <div class="form-group">
     <label for="book_title">Title</label>  <%#<h5>Title</h5> %>
     <input class="form-control book_title" type="text" name="book[title]" id="book_title"> <%# <%= f.text_field :title %>
    </div>
   <div class="form-group">
     <label for="book_opinion">Opinion</label> <%# <h5>Opinion</h5> %>
     <textarea class="form-control book_body" name="book[body]" id="book_body"></textarea>  <%# <%= f.text_area :body %>
    </div>
   <div class="form-group">
     <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book">
   <%# <p><%= f.submit 'Create Book' %>
   </div>
   </form>
  </div>

<div class="col-md-8 offset-md-1">
<h2>Users</h2>
<table class="table">
    <thead>
        <tr>
            <th>image</th>
            <th>name</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <tbody>
       <% @users.each do |user| %>
    　 <tr>
    　　 <td><%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: 'no_image.jpg', size:'50x50', class: "attachment user profile_image fallback" %></td>
    　　  <%#確認画像の表示 %>
       <td> <%= user.name %> </td>
       <td><%= link_to "Show",user_path(user.id) %></td>
    　 </tr>
    　　<% end %>
    </tbody>
</table>
</div>
</div>
</div>

</main>